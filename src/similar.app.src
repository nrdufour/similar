{application, similar,
    [{description, "Embryo Systems - Similar"},
     {vsn, "0.0.1"},
     {modules, [similar,
                similar_sup,
		similar_events,
		similar_process,
		similar_query,
		similar_engine,
		similar_logger,
		similar_log,
	        similar_utils
     ]},
     {registered, []},
     {applications, [kernel, stdlib]},
     {mod, {similar, []}},
     {start_phases, []},
     {env, [ {conf, "./similar.conf"} ]}
]}.

